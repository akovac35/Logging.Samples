{
  "Logging": {
    "_comment": "This section is only needed for NLog which monitors this file for changes. Executing dotnet run will monitor the root file (not in bin), executing dotnet publish will correctly monitor the published file",
    "IncludeScopes": true,
    "LogLevel": {
      "Default": "Trace",
      "Microsoft": "Information"
    }
  },
  "AllowedHosts": "*"
}
